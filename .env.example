# ============================================================
# GUILDED — Environment Variables
# Copy this file to .env and fill in your values.
# NEVER commit .env to version control.
# ============================================================

# ----- Database -----
# PostgreSQL connection string
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/guilded?schema=public"

# ----- NextAuth -----
# Generate with: openssl rand -base64 32
AUTH_SECRET="your-nextauth-secret-here"

# Public app URL (no trailing slash)
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# ----- Google OAuth (Optional) -----
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# ----- OpenAI -----
OPENAI_API_KEY="sk-..."

# ----- Stripe -----
# Secret key (server-only, never expose to client)
STRIPE_SECRET_KEY="sk_test_..."

# Webhook signing secret (from Stripe Dashboard > Webhooks)
STRIPE_WEBHOOK_SECRET="whsec_..."

# Stripe Price IDs for subscription tiers
# Create these in Stripe Dashboard > Products
STRIPE_PRICE_JOURNEYMAN="price_..."   # $9/month
STRIPE_PRICE_MASTER="price_..."       # $39/month
STRIPE_PRICE_HERO="price_..."         # $79/month

# ============================================================
# STRIPE SETUP NOTES:
#
# 1. Create 3 subscription products in Stripe:
#    - Journeyman: $9/month recurring
#    - Master: $39/month recurring
#    - Hero: $79/month recurring
#
# 2. Configure webhook endpoint in Stripe Dashboard:
#    URL: https://your-domain.com/api/stripe/webhook
#    Events to listen for:
#      - customer.subscription.created
#      - customer.subscription.updated
#      - customer.subscription.deleted
#      - invoice.payment_succeeded
#      - invoice.payment_failed
#      - checkout.session.completed
#
# 3. Enable Stripe Billing Portal in Dashboard
#
# ============================================================
